package com.crazybars.crazybars;

import javafx.application.Application;

/*

FOLLOW THESE INSTRUCTIONS TO SET UP GIT

 Initialize the local directory as a Git repository (sets default branch to 'main')
git init -b main


git remote add origin https://github.com/milesbaack/crazy-bars

git push -u origin main
*/

public class Launcher {
    public static void main(String[] args) {
        Application.launch(CrazyBars.class, args);
    }
}
